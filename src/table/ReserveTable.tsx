// import { Route, Routes } from "react-router-dom";
// import ReserveForm from "../forms/ReserveForm";
// import { useAppSelector } from "../redux/hooks";
// import Table from "./table-ui/Table";

// type MTProps = {
//   link: string;
// };

// export default function ReserveTable<T>(p: MTProps) {
//   const reserveData = useAppSelector((s) => s.reserve.reserve);
//   const reserveTableColumns = useAppSelector((c) => c.reserve.columns);
//   return <Table columns={reserveTableColumns} data={reserveData} />;
// }
